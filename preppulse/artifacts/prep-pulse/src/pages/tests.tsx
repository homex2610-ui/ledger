import { useState, type FormEvent } from 'react';
import { BarChart3, CalendarDays, ChevronDown, Plus, X } from 'lucide-react';
import { useQueryClient } from '@tanstack/react-query';
import { getListTestAttemptsQueryKey, useAnalyzeTestAttempts, useCreateTestAttempt, useListTestAttempts } from '@workspace/api-client-react';
import type { TestAttempt, TestAttemptInputExam } from '@workspace/api-client-react';
import { Card, EmptyState, ErrorState, LoadingBlock, SectionTitle } from '@/components/ui-elements';

const subjectOptions = ['', 'Physics', 'Chemistry', 'Mathematics', 'Biology', 'Full syllabus'];

export default function Tests() {
  const queryClient = useQueryClient();
  const query = useListTestAttempts();
  const analyzeQuery = useAnalyzeTestAttempts();
  const createTest = useCreateTestAttempt();
  const [showForm, setShowForm] = useState(false);
  const [form, setForm] = useState({ name: '', exam: 'jee_main' as TestAttemptInputExam, subject: '', score: '', maxScore: '300', attempted: '', totalQuestions: '90', timeMinutes: '180', negativeMarksLost: '0' });
  const attempts = query.data ?? [];
  const average = attempts.length ? Math.round(attempts.reduce((sum, attempt) => sum + attempt.accuracy, 0) / attempts.length) : 0;
  const best = attempts.length ? Math.max(...attempts.map((attempt) => attempt.accuracy)) : 0;
  const handleSubmit = (event: FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    createTest.mutate({ data: { name: form.name, exam: form.exam, subject: form.subject || undefined, score: Number(form.score), maxScore: Number(form.maxScore), attempted: Number(form.attempted), totalQuestions: Number(form.totalQuestions), timeMinutes: Number(form.timeMinutes), negativeMarksLost: Number(form.negativeMarksLost) } }, { onSuccess: () => { queryClient.invalidateQueries({ queryKey: getListTestAttemptsQueryKey() }); analyzeQuery.refetch(); setShowForm(false); setForm({ name: '', exam: 'jee_main', subject: '', score: '', maxScore: '300', attempted: '', totalQuestions: '90', timeMinutes: '180', negativeMarksLost: '0' }); } });
  };

  if (query.isLoading) return <div className="mx-auto max-w-6xl"><LoadingBlock className="h-24" /><div className="mt-6 grid gap-6 lg:grid-cols-[1.2fr_.8fr]"><LoadingBlock className="h-72" /><LoadingBlock className="h-72" /></div></div>;
  if (query.isError) return <div className="mx-auto max-w-6xl"><ErrorState onRetry={() => query.refetch()} /></div>;

  return <div className="mx-auto max-w-6xl">
    <div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="font-mono-custom text-[10px] uppercase tracking-[.18em] text-primary">Evidence, not ego</p><h1 className="mt-2 font-display text-4xl font-bold tracking-[-.04em] md:text-5xl">Test log</h1><p className="mt-3 max-w-xl text-sm text-muted-foreground">A test is a map of what to fix next, never a verdict on you.</p></div><button type="button" onClick={() => setShowForm((open) => !open)} className="inline-flex w-fit items-center gap-2 rounded-xl bg-primary px-4 py-3 text-sm font-bold text-primary-foreground transition-transform hover:-translate-y-0.5" data-testid="button-log-test"><Plus size={16} /> Log a test</button></div>
    {showForm && <Card className="mt-7 border-primary/25 p-5 md:p-7"><div className="mb-5 flex items-center justify-between"><div><p className="font-mono-custom text-[10px] uppercase tracking-[.16em] text-primary">New attempt</p><h2 className="mt-1 font-display text-xl font-bold">Add the useful numbers</h2></div><button type="button" onClick={() => setShowForm(false)} className="rounded-lg p-2 text-muted-foreground hover:bg-secondary" aria-label="Close form" data-testid="button-close-test-form"><X size={17} /></button></div><form onSubmit={handleSubmit} className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Field label="Test name" value={form.name} onChange={(value) => setForm({ ...form, name: value })} placeholder="e.g. Allen Major 04" testId="input-test-name" required /><label className="block"><span className="mb-1.5 block text-xs font-bold">Exam</span><select value={form.exam} onChange={(event) => setForm({ ...form, exam: event.target.value as TestAttemptInputExam })} className="h-10 w-full rounded-xl border border-border bg-background px-3 text-sm outline-none" data-testid="select-test-exam"><option value="jee_main">JEE Main</option><option value="neet">NEET</option></select></label><label className="block"><span className="mb-1.5 block text-xs font-bold">Subject</span><select value={form.subject} onChange={(event) => setForm({ ...form, subject: event.target.value })} className="h-10 w-full rounded-xl border border-border bg-background px-3 text-sm outline-none" data-testid="select-test-subject"><option value="">Full syllabus</option>{subjectOptions.slice(1).map((subject) => <option key={subject} value={subject}>{subject}</option>)}</select></label><Field label="Score" value={form.score} onChange={(value) => setForm({ ...form, score: value })} placeholder="178" testId="input-test-score" type="number" required /><Field label="Max score" value={form.maxScore} onChange={(value) => setForm({ ...form, maxScore: value })} placeholder="300" testId="input-test-max-score" type="number" required /><Field label="Attempted" value={form.attempted} onChange={(value) => setForm({ ...form, attempted: value })} placeholder="72" testId="input-test-attempted" type="number" required /><Field label="Total questions" value={form.totalQuestions} onChange={(value) => setForm({ ...form, totalQuestions: value })} placeholder="90" testId="input-test-total" type="number" required /><Field label="Time (minutes)" value={form.timeMinutes} onChange={(value) => setForm({ ...form, timeMinutes: value })} placeholder="180" testId="input-test-time" type="number" required /><Field label="Negative marks lost" value={form.negativeMarksLost} onChange={(value) => setForm({ ...form, negativeMarksLost: value })} placeholder="4" testId="input-test-negative" type="number" required /><div className="flex items-end sm:col-span-2 lg:col-span-4"><button type="submit" disabled={createTest.isPending || !form.name} className="rounded-xl bg-accent px-5 py-3 text-sm font-bold text-accent-foreground disabled:cursor-not-allowed disabled:opacity-50" data-testid="button-submit-test">{createTest.isPending ? 'Saving attempt...' : 'Save attempt'}</button></div></form></Card>}
    <div className="mt-7 grid gap-3 sm:grid-cols-3"><MiniStat label="Attempts logged" value={String(attempts.length)} detail="keep the sample honest" /><MiniStat label="Average accuracy" value={`${average}%`} muted={attempts.length < 5} detail={attempts.length < 5 ? `based on ${attempts.length} attempt${attempts.length === 1 ? '' : 's'} — keep the sample honest` : 'across your attempts'} /><MiniStat label="Best accuracy" value={`${best}%`} muted={attempts.length < 5} detail={attempts.length < 5 ? `a single attempt can flatter — log more to trust it` : 'a signal worth repeating'} /></div>
    <div className="mt-7 grid gap-6 lg:grid-cols-[.8fr_1.2fr]">
      <Card className="p-5 md:p-7"><SectionTitle eyebrow="Trend line" title="Accuracy over time" action={<BarChart3 size={18} className="text-primary" />} /><div className="mt-7 flex h-48 items-end gap-2 border-b border-border/70 pb-0">{attempts.length ? attempts.slice().reverse().map((attempt, index) => <div key={attempt.id} className="group flex h-full flex-1 flex-col items-center justify-end gap-2"><span className="font-mono-custom text-[10px] text-primary opacity-0 transition-opacity group-hover:opacity-100">{attempt.accuracy}%</span><div className="w-full max-w-10 rounded-t-lg bg-primary/75 transition-colors group-hover:bg-accent" style={{ height: `${Math.max(8, attempt.accuracy)}%` }} /><span className="font-mono-custom text-[9px] text-muted-foreground">{index + 1}</span></div>) : <p className="mb-6 text-sm text-muted-foreground">Log one attempt to start your trend.</p>}</div></Card>
      <Card className="overflow-hidden"><div className="p-5 pb-3 md:p-7 md:pb-4"><SectionTitle eyebrow={`${attempts.length} logged`} title="Recent attempts" /></div>{attempts.length ? <div className="divide-y divide-border/70">{attempts.map((attempt) => <TestRow key={attempt.id} attempt={attempt} />)}</div> : <div className="px-5 pb-6"><EmptyState title="No tests logged yet" detail="Your first attempt turns uncertainty into a plan." action={<button type="button" onClick={() => setShowForm(true)} className="rounded-xl bg-primary px-4 py-2 text-xs font-bold text-primary-foreground" data-testid="button-empty-log-test">Log your first test</button>} /></div>}</Card>
    </div>

    <div className="mt-6 grid gap-6 lg:grid-cols-[.9fr_1.1fr]">
      <Card className="p-5 md:p-7"><SectionTitle eyebrow="Where to look next" title="Weak areas" /><div className="mt-4 flex flex-wrap gap-2">{analyzeQuery.data?.weakAreas.length ? analyzeQuery.data.weakAreas.map((area) => <span key={area.name} className="rounded-full border border-accent/25 bg-accent/10 px-3 py-1.5 font-mono-custom text-[10px] font-bold uppercase tracking-[.1em] text-accent">{area.name} <span className="text-accent/70">×{area.count}</span></span>) : <p className="text-sm text-muted-foreground">Weak areas will appear here once you log attempts with subject data.</p>}</div></Card>
      <Card className="p-5 md:p-7"><SectionTitle eyebrow="By subject" title="Average accuracy" /><div className="mt-4 space-y-3">{analyzeQuery.data?.subjectAverages.length ? analyzeQuery.data.subjectAverages.map((subject) => <div key={subject.subject} className="flex items-center gap-3"><span className="w-32 truncate text-xs font-bold">{subject.subject}</span><div className="h-2.5 flex-1 rounded-full bg-secondary"><div className="h-2.5 rounded-full bg-primary" style={{ width: `${subject.averageAccuracy}%` }} /></div><span className="w-10 text-right font-mono-custom text-xs font-bold text-primary">{subject.averageAccuracy}%</span></div>) : <p className="text-sm text-muted-foreground">Subject averages appear once attempts are logged.</p>}</div></Card>
    </div>
  </div>;
}

function Field({ label, value, onChange, placeholder, testId, type = 'text', required = false }: { label: string; value: string; onChange: (value: string) => void; placeholder: string; testId: string; type?: string; required?: boolean }) {
  return <label className="block"><span className="mb-1.5 block text-xs font-bold">{label}</span><input required={required} type={type} min={type === 'number' ? 0 : undefined} value={value} onChange={(event) => onChange(event.target.value)} placeholder={placeholder} className="h-10 w-full rounded-xl border border-border bg-background px-3 text-sm outline-none focus:ring-3 focus:ring-primary/20" data-testid={testId} /></label>;
}
function MiniStat({ label, value, detail, muted = false }: { label: string; value: string; detail: string; muted?: boolean }) { return <Card className="p-4"><p className="font-mono-custom text-[10px] uppercase tracking-[.15em] text-muted-foreground">{label}</p><p className={`mt-2 font-display text-2xl font-bold ${muted ? 'text-muted-foreground' : ''}`}>{value}</p><p className="mt-1 text-xs text-muted-foreground">{detail}</p></Card>; }
function TestRow({ attempt }: { attempt: TestAttempt }) {
  return <div className="flex items-center gap-3 px-5 py-4 md:px-7" data-testid={`row-test-${attempt.id}`}><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-secondary text-primary"><CalendarDays size={16} /></div><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold">{attempt.name}</p><p className="text-xs text-muted-foreground">{attempt.exam === 'jee_main' ? 'JEE Main' : 'NEET'} · {new Date(attempt.date).toLocaleDateString(undefined, { month: 'short', day: 'numeric' })}</p></div><div className="text-right"><p className="font-display font-bold">{attempt.accuracy}%</p><p className="font-mono-custom text-[10px] text-muted-foreground">{attempt.score}/{attempt.maxScore}</p></div><ChevronDown size={15} className="rotate-[-90deg] text-muted-foreground" /></div>;
}